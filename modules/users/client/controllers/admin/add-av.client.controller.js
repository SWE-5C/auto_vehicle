'use strict';//CONTROLLER THAT CONTROLS the add av view that was not implemented

angular.module('users.admin').controller('addAVController', ['$scope', function($scope){
  $scope.id = {
    text: 'id'
  };
}]);
